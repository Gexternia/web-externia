{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run dev",
      "name": "SvelteKit dev",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
